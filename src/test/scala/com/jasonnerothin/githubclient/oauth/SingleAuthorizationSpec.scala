package com.jasonnerothin.githubclient.oauth

import org.scalatest._

/**
 * Created by IntelliJ IDEA.
 * User: jason
 * Date: 7/10/13
 * Time: 10:51 PM
 */
class SingleAuthorizationSpec extends FunSuite{

  test("login returns a token on happy path") (pending)

  test("login returns None when we use a bad client_id") (pending)

  test("login returns None when we use a bad client_secret") (pending)

  test("login returns None when we use a bad username") (pending)

  test("login returns None when we use a bad password") (pending)

  test("login returns a token on happy path") (pending)

  test("stillAuthorized returns false when github returns a 404") (pending)

  test("stillAuthorized returns true when server says we're authorized") (pending)

}
